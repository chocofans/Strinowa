<Window x:Class="StrinowaWPF.Bookmarks"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="540" Height="620" MinWidth="420" MinHeight="480"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Title="Strinowa bookmarks">
    <Window.Resources>
        <Style x:Key="BookmarkButton" TargetType="Button">
            <Setter Property="Margin" Value="0,0,0,7"/>
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="{DynamicResource ControlTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ControlSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="1.012" ScaleY="1.012"/></Setter.Value></Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98"/></Setter.Value></Setter>
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.78"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border x:Name="RootBorder" CornerRadius="12" BorderThickness="1" Padding="16"
            Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource WindowBorderBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Bookmarks" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource ControlTextBrush}"/>
                    <TextBlock Text="Choose a common branch to index on PC" Margin="0,3,0,0" Foreground="{DynamicResource MutedTextBrush}" FontSize="11"/>
                </StackPanel>
                <Button x:Name="CloseButton" HorizontalAlignment="Right" Width="28" Height="28" Content="×"
                        FontSize="18" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource MutedTextBrush}" Click="CloseButton_Click"/>
            </Grid>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <TextBlock Text="OVERSEAS" Foreground="{DynamicResource AccentBrush}" FontFamily="Cascadia Mono" FontWeight="Bold" FontSize="11" Margin="0,0,0,6"/>
                    <TextBlock Text="Game branches" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,0,0,5"/>
                    <StackPanel x:Name="OverseasGame"/>
                    <TextBlock Text="Launcher branches" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,7,0,5"/>
                    <StackPanel x:Name="OverseasLauncher"/>
                    <Separator Margin="0,8,0,12" Background="{DynamicResource WindowBorderBrush}"/>
                    <TextBlock Text="CN" Foreground="{DynamicResource AccentBrush}" FontFamily="Cascadia Mono" FontWeight="Bold" FontSize="11" Margin="0,0,0,6"/>
                    <TextBlock Text="Game branches" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,0,0,5"/>
                    <StackPanel x:Name="CnGame"/>
                    <TextBlock Text="Launcher branches" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,7,0,5"/>
                    <StackPanel x:Name="CnLauncher"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
